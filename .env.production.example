# 服务器配置
# 生产环境端口，默认为5000
PORT=5000
# 务必设置为production
NODE_ENV=production

# 数据库配置
# 格式: mongodb://[用户名:密码@]数据库地址:端口/数据库名
MONGODB_URI=mongodb://username:password@localhost:27017/anwei

# JWT密钥
# 请使用安全的随机字符串，建议长度至少32位
JWT_SECRET=your_secure_jwt_secret_key_here

# 邮件配置
EMAIL_HOST=smtp.example.com
EMAIL_PORT=465
EMAIL_USER=your_email@example.com
EMAIL_PASS=your_email_password_or_app_password

# 通义千问API配置
QWEN_API_KEY=your_qwen_api_key
QWEN_API_URL=https://dashscope.aliyuncs.com/api/v1

# 可选配置
# 最大上传文件大小(MB)
MAX_UPLOAD_SIZE=20
# 缓存 duration(秒)
CACHE_DURATION=3600

# 安全配置
# 启用HTTPS时设置为true
FORCE_HTTPS=true

# 日志级别
# 可选: error, warn, info, debug
LOG_LEVEL=info

# 请根据实际情况修改以上配置
# 生产环境中不要使用此文件，而是创建一个新的.env文件
# 并确保.env文件已添加到.gitignore中